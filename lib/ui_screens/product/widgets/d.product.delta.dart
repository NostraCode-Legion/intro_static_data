part of '../_index.dart';

class ProductDelta extends StatelessWidget {
  const ProductDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
