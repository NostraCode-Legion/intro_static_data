part of '../_index.dart';

class ProductEcho extends StatelessWidget {
  const ProductEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
